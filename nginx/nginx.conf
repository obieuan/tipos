server {
    listen 80;
    server_name tiposchelem.com www.tiposchelem.com;

    # Let's Encrypt challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirect all HTTP to HTTPS
    location / {
        return 301 https://$host$request_uri;
    }
}

# HTTPS - uncomment after obtaining SSL certificate
#server {
#    listen 443 ssl;
#    server_name tiposchelem.com www.tiposchelem.com;
#
#    ssl_certificate /etc/letsencrypt/live/tiposchelem.com/fullchain.pem;
#    ssl_certificate_key /etc/letsencrypt/live/tiposchelem.com/privkey.pem;
#
#    client_max_body_size 5M;
#
#    location / {
#        proxy_pass http://app:5000;
#        proxy_set_header Host $host;
#        proxy_set_header X-Real-IP $remote_addr;
#        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#        proxy_set_header X-Forwarded-Proto $scheme;
#    }
#
#    location /static/ {
#        proxy_pass http://app:5000/static/;
#        proxy_cache_valid 200 1d;
#        add_header Cache-Control "public, max-age=86400";
#    }
#}
